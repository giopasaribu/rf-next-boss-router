{
  "name": "rf-next-boss-router",
  "version": "1.0.0",
  "description": "RF Boss Announcement Router — a single-maintainer admin UI to manage guilds, a recurring boss schedule, and per-guild Discord + Telegram announcements with automatic pre-spawn reminders (times in WIB / UTC+7).",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc && node scripts/copy-assets.mjs",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@fastify/static": "^8.0.3",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
